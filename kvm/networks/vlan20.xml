<!--
  ============================================================
  VLAN 20 — Zone DMZ (services publics)
  ============================================================
  Réseau KVM isolé (pas de <forward>, pas de <ip>).
  OPNsense (10.10.20.1) est le SEUL à posséder une IP.
  ============================================================
-->
<network>
  <name>vlan20</name>
  <bridge name="virbr20" stp="off" delay="0"/>
</network>
