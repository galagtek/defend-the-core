<!--
  ============================================================
  VLAN 99 — Zone Admin / SIEM (très restreint)
  ============================================================
  Réseau KVM isolé (pas de <forward>, pas de <ip>).
  OPNsense (10.10.99.1) est le SEUL à posséder une IP.
  ============================================================
-->
<network>
  <name>vlan99</name>
  <bridge name="virbr99" stp="off" delay="0"/>
</network>
