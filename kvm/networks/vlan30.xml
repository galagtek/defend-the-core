<!--
  ============================================================
  VLAN 30 — Zone Critique (serveurs métier, BDD)
  ============================================================
  Réseau KVM isolé (pas de <forward>, pas de <ip>).
  OPNsense (10.10.30.1) est le SEUL à posséder une IP.
  ============================================================
-->
<network>
  <name>vlan30</name>
  <bridge name="virbr30" stp="off" delay="0"/>
</network>
